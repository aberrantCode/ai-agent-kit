---
description: Alias of /update-service — update an existing OPBTA service on svc.opbta.com (bump/reconcile image, config, ports, env, or exposure). Matches the opbta-service skill's /opbta-* command family.
---

Alias of `/update-service`.

Apply the `opbta-service` skill and execute its **update** operation, using the current working
directory as the source-repo context and any trailing message as the target slug / change
description. See `/update-service` for the full resolution order and autonomy model.
