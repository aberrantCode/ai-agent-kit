---
description: Alias for the compliance-audit skill — audit Claude Code session history for whether a stated policy or rule was actually followed in practice, not just stated
---

Load the `session-handoff` skill (`Skill(session-handoff)`), then **read and follow** `sub-skills/audit/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(session-handoff:audit)`.
