---
description: Turn this session's open decision items into a handoff prompt and launch it in a fresh Claude session in a new Windows Terminal tab
---

Load the `session-handoff` skill (`Skill(session-handoff)`), then **read and follow** `sub-skills/handoff/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(session-handoff:handoff)`.
