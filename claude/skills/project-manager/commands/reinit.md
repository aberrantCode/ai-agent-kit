---
description: Archive legacy plans and tasks, normalize feature specs to the canonical template, then launch the orchestration loop. Use when recovering from inconsistent state.
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/reinit/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:reinit)`.
