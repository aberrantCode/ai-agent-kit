---
description: Analyze whether project-manager tasks are safe for an explicit future parallel batch without starting workers
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/analyze-parallelism/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:analyze-parallelism)`.
