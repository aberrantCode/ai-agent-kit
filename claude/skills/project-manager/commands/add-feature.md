---
description: Create a new feature spec — interviews user, detects overlap with existing specs, populates the canonical template, generates a diagram, and updates the feature index
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/add-feature/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:add-feature)`.
