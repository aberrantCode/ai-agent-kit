---
description: Audit all feature specs in docs/features/ against the canonical template, CAP-ID standards, open questions, logical gaps, and plan coverage — then request user authorization before making any changes
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/analyze-features/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:analyze-features)`.
