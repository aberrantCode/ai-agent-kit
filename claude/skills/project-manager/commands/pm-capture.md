---
description: Front-door intake — capture a bug, chore, tech-debt, or idea into the backlog
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/capture/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:capture)`.
