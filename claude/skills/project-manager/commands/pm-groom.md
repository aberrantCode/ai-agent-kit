---
description: Triage and promote a backlog item or homeless issue into the feature or chore lane
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/groom/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:groom)`.
