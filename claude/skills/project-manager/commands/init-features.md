---
description: Run the Feature Interview — extract functional areas from docs/REQUIREMENTS.md, interview the user one area at a time, and write feature specs to docs/features/
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/init-features/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:init-features)`.
