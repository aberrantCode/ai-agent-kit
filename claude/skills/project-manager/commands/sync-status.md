---
description: Regenerate docs/STATUS.md — the single outstanding-work + next-action tracker — from the canonical specs/plans/tasks/issues, preserving the curated sections
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/sync-status/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:sync-status)`.
