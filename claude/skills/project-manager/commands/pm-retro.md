---
description: Harvest close-out learnings from archived tasks and resolved issues into the workflow ledger, deduped
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/retro/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:retro)`.
