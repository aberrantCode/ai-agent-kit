---
description: Express chore-task fast path — capture and materialize a chore task in one shot
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/task/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:task)`.
