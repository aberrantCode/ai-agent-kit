---
description: Optionally mirror local project-manager plans and tasks to GitHub issues while keeping markdown authoritative
---

Load the `project-manager` skill (`Skill(project-manager)`), then **read and follow** `sub-skills/sync-tracker/SKILL.md`. The sub-skill is a file in the loaded bundle to read -- not a skill to dispatch; do not call `Skill(project-manager:sync-tracker)`.
