---
description: Spawn a new Windows Terminal tab running an arbitrary command or script, in a folder you choose, colored to match its repo
---

Invoke the `spawn-terminal` skill and follow it exactly.
