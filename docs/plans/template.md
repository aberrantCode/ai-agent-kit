---
feature: "{{feature-slug}}"
status: in-progress      # not-started | in-progress | done | blocked
failures: 0              # used by the Error Recovery Loop; do not edit by hand
last_updated: "2026-07-27"
external_issue: ""
external_url: ""
---

# Plan — {{feature-slug}}

> **Living checklist.** This file is both the specification and the progress tracker. The orchestrator (`/continue-tasks`) reads the Status column to decide what to do next. Agents do not edit this file directly; they edit task files in `docs/tasks/active/` and the orchestrator reconciles status back here.

> **▣ Diagram —** Agent ↔ orchestrator task handoff *(type: swimlane)*

**Source spec:** `docs/features/{{feature-slug}}.md`

> The `Covers` column below cites a CAP-ID (`XX-CAP-NN`) — see the [Feature
> Abbreviation Registry](../workflow/SDLC.md#feature-abbreviation-registry).

---

## Phase 1 — {{Phase 1 name}}

**Goal.** One sentence describing what is true at the end of this phase that wasn't true at the start.

**Exit criteria.** Bullet list of observable outcomes (tests passing, file present, endpoint reachable, etc.).

| # | Task | Covers | Role | Status | Notes |
|---|------|--------|------|--------|-------|
| 1 | Describe the task imperatively | `XX-CAP-01` | `tdd-guide` | todo | |
| 2 | Another task | `XX-CAP-01` | `code-reviewer` | todo | |

---

## Phase 2 — {{Phase 2 name}}

**Goal.** ...

**Exit criteria.** ...

| # | Task | Covers | Role | Status | Notes |
|---|------|--------|------|--------|-------|
| 1 | ... | `XX-CAP-02` | `planner` | todo | |

---

## Role legend

| Role keyword              | Agent type           |
|---------------------------|----------------------|
| architecture, design, planning | `planner`         |
| feature, implementation, api   | `tdd-guide`       |
| review, quality                | `code-reviewer`   |
| security                       | `security-reviewer` |
| build, types, errors           | `build-error-resolver` |
| e2e, testing                   | `e2e-runner`      |
| docs, documentation            | `doc-updater`     |
| cleanup, refactor              | `refactor-cleaner` |
| _anything else_                | `general-purpose` |

---

## Status legend

> This mirrors [`../workflow/SDLC.md`](../workflow/SDLC.md#status-vocabulary)'s
> Status Vocabulary table, which is the authoritative copy (SDLC.md says as much);
> treat that one as the source of truth if the two ever drift.

| Status        | Meaning                                               |
|---------------|-------------------------------------------------------|
| `todo`        | Eligible to be picked up by `/continue-tasks`         |
| `in-progress` | Task file exists in `docs/tasks/active/`              |
| `done`        | Completion sentinel observed, required verification passed, task file archived |
| `blocked`     | Blocker issue written and surfaced to user; do not auto-retry |

Implementation, API, build, security, UI, and other code-changing tasks require verification before
they are final. If the plan does not already include a matching review, test, or quality task for the
same CAP-ID, `/continue-tasks` inserts one after the implementation task succeeds.

## Optional task metadata

Use these fields in generated task files, or in plan notes when planning future work, only when they
are needed:

| Field | Meaning |
|-------|---------|
| `external_issue` / `external_url` | Optional GitHub issue mirror maintained by `/sync-tracker`; local markdown remains authoritative |
| `claimed_by`, `claimed_at`, `lease_expires_at` | Coordination lease for active work |
| `parallel` | Defaults to `false`; only `true` after explicit parallelism analysis |
| `depends_on_tasks` | Local task ids that must be complete first |
| `conflicts_with` | Local task ids that cannot run in the same batch |
| `files_allowed` | File globs this task may own |
| `files_shared` | File globs requiring batch-level coordination |
