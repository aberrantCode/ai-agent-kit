{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "pwsh -NoProfile -File scripts/guard-pm-flow.ps1 --stage pre-tool || exit 0",
            "_note": "Guard runs in advisory mode at pre-tool — Git pre-commit is the hard gate. Pre-tool exits 0 unconditionally so it never breaks interactive editing; it warns when an Edit lands outside an active task scope."
          }
        ]
      }
    ]
  }
}
